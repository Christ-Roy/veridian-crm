/*
 * Veridian CRM — module SSO clean-room (AGPLv3).
 * Réécrit depuis les protocoles standards SAML 2.0 / OIDC, sans code EE Twenty.
 */
export enum VeridianSsoProviderType {
  SAML = 'SAML',
  OIDC = 'OIDC',
}
